---
name: "Jellyfin Xtream"
guid: "5d774c35-8567-46d3-a950-9bb8227a0c5d"
version: "0.9.41.0"
targetAbi: "10.11.0.0"
framework: "net9.0"
overview: "Stream content from an Xtream-compatible server."
description: >
  Stream Live IPTV, Video On-Demand, and Series from an Xtream-compatible server using this plugin.
category: "LiveTV"
owner: "Kevinjil"
artifacts:
  - "Jellyfin.Xtream.dll"
changelog: >
  Fix recording playback failure (ffmpeg exit 234): delay library notification until
  3 HLS segments are ready so ffprobe has enough data to analyze. Fix recording file
  cleanup so .ts, .strm, and segment files are always removed. Add ffprobe/ffmpeg
  validation to integration tests (28 assertions).
